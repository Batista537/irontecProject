export enum Icons {
    status = 'status',
    statusOpen = 'statusOpen',
    statusClose = 'statusClose',
    noResults = 'noResults',
    networkOff = 'networkOff',
    networkOn = 'networkOn',
    Close = 'close'
}
